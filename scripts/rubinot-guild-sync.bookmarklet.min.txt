javascript:(function(){ const API_BASE = "http://localhost:3001"; const INTERNAL_TOKEN = "local-dev-internal-token-change-me"; function badge(text, color) { let b = document.getElementById("wl-sync-badge"); if (!b) { b = document.createElement("div"); b.id = "wl-sync-badge"; b.style.cssText = "position:fixed;bottom:12px;right:12px;z-index:999999;padding:8px 14px;border-radius:8px;font:12px/1.4 sans-serif;color:#fff;box-shadow:0 2px 10px rgba(0,0,0,.4);"; document.body.appendChild(b); } b.textContent = text; b.style.background = color; } function waitForTable(cb) { const start = Date.now(); (function check() { const rows = document.querySelectorAll("table tbody tr").length; if (rows > 0 || Date.now() - start > 15000) return cb(rows); setTimeout(check, 300); })(); } badge("⏳ Sincronizando...", "#2563eb"); waitForTable(function (rowCount) { if (rowCount === 0) { badge("⚠️ Nenhuma tabela encontrada", "#d97706"); return; } fetch(API_BASE + "/internal/ingest/guild-html", { method: "POST", headers: { "Content-Type": "application/json", "x-internal-token": INTERNAL_TOKEN }, body: JSON.stringify({ html: document.documentElement.outerHTML }), }) .then(function (res) { return res.json().then(function (body) { if (res.ok) badge("✅ Sync OK (" + (body.members ?? "?") + " membros)", "#16a34a"); else badge("❌ Erro " + res.status, "#dc2626"); }); }) .catch(function (err) { badge("❌ Falha ao conectar na API", "#dc2626"); console.error("[Warfire Sync]", err); }); }); })();