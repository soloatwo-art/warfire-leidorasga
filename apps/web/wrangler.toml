name = "warfire-leidorasga-web"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# Set these in the Cloudflare Pages dashboard (Settings > Environment variables)
# instead of here, since they differ between Preview and Production:
#   NEXT_PUBLIC_API_URL = https://api.<your-domain>.com  (or the Container's public URL)
#   NEXT_PUBLIC_WS_URL  = same as above (Socket.IO shares the HTTP origin)
